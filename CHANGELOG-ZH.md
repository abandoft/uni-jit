## 0.2.5

- 新增一套面向64位AArch64、x86-64与RISC-V 64主机、完全独立设计的版本化C17商业嵌入ABI，不暴露或复用SLJIT及任何C++实现布局：提供67个`unijit_v1_*`共享库入口、不透明且所有权明确的builder/function/compiler/compiled generation/execution context/code cache/code handle/error、固定宽度可扩展的ABI/目标/限额/统计结构、精确的Word与Float64值位签名、标量构建和编译、guard与可计数safepoint、协作式中断和可诊断退出、目标档案验证、有界编译与缓存预算、代次安全的类型化快速调用绑定、不可变代码的数据补丁单元发布、缓存查找/失效/保留代次语义、结构化状态码、1024字节有界诊断以及完整异常隔离；新增C17聚合布局冻结、畸形结构/保留字段/签名/资源限额负向验证、标量/Float64/safepoint/缓存/补丁单元/快速调用生命周期资格测试、与C++消费者并列的纯C安装包消费者、精确导出清单与泄漏检查器，以及在真实Ubuntu GCC x86-64、Windows MSVC x86-64和Apple Clang AArch64主机上职责明确的共享库流水线；在CFG、类型化内存与对象、原子、SIMD值、恢复、分层、调度、可移植IR和AOT的完整版本化契约交付前，这些能力不会通过该ABI开放。

## 0.2.4

- 为直线与 CFG IR 新增面向上下文无关 Word/Float64 目标的代次安全类型化 JIT 到 JIT 内部调用，覆盖独立受限且经过验证的调用描述符、显式解释器 oracle 绑定、副作用安全优化、通过 release/acquire 发布的不可变目标表快照、精确已编译代次缓存租约、未绑定与托管入口失效关闭、严格签名与目标档案校验、安全缓存失效和并发目标切换，以及 AArch64、x86-64 System V/Windows 与 RISC-V 64 上独立的间接原生入口降级；补齐安装包消费、400000 次调用/10000 次切换扩展压力、ThreadSanitizer 覆盖，以及将完整 CFG 循环限制为等价运行时 helper 延迟 1.5 倍以内、调用者 256 字节以内、目标 64 字节以内的留存性能门槛；完整契约已通过包含 Ubuntu 与 Windows x86-64、Linux/Windows Sanitizer 的 13 任务托管流水线、另一台真实 Ubuntu GCC 13.3 x86-64 主机和真实 RISC-V 64 GCC 14.2 硬件验证，同时让需要上下文、嵌套、GC 引用、寄存器前缀和尾调用的目标继续失效关闭并留待后续契约。
- 新增完全独立的严格 128 位可移植 SIMD 语义核心，包含六种整数与浮点 lane 类型、规范化全零/全一掩码向量、低地址优先 lane 顺序、保持位模式的常量、零值与标量广播、lane 提取与插入、回绕整数算术、零除数行为确定的整数除法、IEEE-754 浮点算术、按位运算、兼顾有序与无序情形的比较、掩码选择、lane 符号掩码、常量 shuffle，以及有符号或无符号整数扩展。
- 将向量值贯通直线与 CFG SSA，覆盖类型化构建器、支配关系与形状验证、位精确参考解释、常量折叠与代数化简、死代码清理、块内值编号、向量块参数、类型化并行边复制、稳定文本格式，以及分别受限的向量常量、shuffle 掩码和活跃向量值。
- 新增默认拒绝的原生编译预检：任何折叠后仍存在的向量操作都会在对应后端降级启用前被拒绝，而完全折叠为标量结果的向量表达式仍可生成标量原生代码；公开标量调用 ABI 继续拒绝向量参数或返回值，并新增类型化 Word/SIMD 直线扫描与 CFG 分配，支持 AArch64 和 x86-64 上无冲突的 Float64/向量共享、显式 RISC-V 64 向量纯驻栈模式、16 字节对齐的溢出与跨调用备份槽、按物理寄存器库感知的 Float64/向量混合并行复制，以及从标量运行时退出捕获中排除非引用向量。
- 通过覆盖全部六种向量类型的 lane 边界、有符号零、无穷、NaN 载荷、掩码、shuffle、扩展、CFG 边、验证器负向、优化器、资源上限、安装包和确定性差分模糊测试验证 SIMD 语义底线，记录规范化可移植契约与能力边界，并通过包含真实 Ubuntu 与 Windows x86-64 主机在内的完整 13 任务托管质量流水线。
- 为当前完整显式严格 128 位操作面新增独立 AArch64 Advanced SIMD/NEON 直线与 CFG 原生降级，覆盖 Float64/向量共享寄存器、完整 128 位对齐溢出与 helper 跨调用保护、混合类型并行边复制、整数与浮点算术、按位逻辑、规范化比较与选择、lane 移动与符号掩码、常量 shuffle、扩展和标量合法化的 I64x2 乘法；在真实 Apple AArch64 主机上通过基线/优化原生码与解释器一致性、主动破坏 SIMD 寄存器、寄存器与 CFG 溢出压力、混合寄存器库环路、ASan/UBSan、提交内 128 程序语料和两轮各 512 程序的扩展确定性差分验证。
- 为 System V 与 Windows x64 新增基于强制基线 SSE2 的独立 x86-64 降级，完整覆盖当前显式严格 128 位操作面，其中打包整数加减与 I16x8 乘法、严格打包浮点算术与有序比较、按位选择、符号掩码提取、32/64 位 shuffle 和扩展采用直接指令，而 SSE2 缺少的操作采用有界标量合法化；同时接入 16 字节对齐向量栈帧、完整宽度溢出与跨调用保存、类型化 CFG 边临时区和 Float64/向量混合环路，并在原生 Rosetta x86-64、真实 Ubuntu GCC/Clang 与 Windows MSVC x86-64、Linux 与 Rosetta ASan/UBSan、ThreadSanitizer 下通过基线/优化层、主动破坏 XMM、压力测试、提交内语料和两轮各 512 程序的扩展确定性差分验证。
- 为当前完整显式严格 128 位操作面新增独立的有界 RV64IMD 标量降级，无需也不宣称 RVV 支持；向量值保留在对齐双字驻栈槽中，并将常量、lane 移动、整数与浮点算术、比较、规范掩码与选择、按位逻辑、符号掩码、shuffle、扩展、溢出、跨 helper 调用和类型化 CFG 边降级为有限标量指令序列；在真实 Bianbu RISC-V 64 硬件上通过基线与优化原生码/解释器一致性、压力路径、提交内 128 程序语料和两轮各 512 程序的扩展确定性差分验证，同时把探测到的 RVV 能力保留给未来按档案选择的快速路径。
- 新增可复现的严格 I32x4 完整 CFG 循环性能门槛，对固定递推的向量原生码、等价标量原生码和向量解释器结果进行逐位校验，保留编译、代码体积、溢出槽、中位延迟、比率和校验和 JSON 证据，并要求相对等价标量生成代码至少加速 1.10 倍、相对解释器至少加速 10 倍且向量代码不超过 1024 字节；该门槛已在托管 AArch64、Ubuntu GCC/Clang 与 Windows MSVC x86-64、托管 macOS x86-64 和真实 Bianbu RISC-V 64 上通过，其中有界标量化仍比等价标量 CFG 快 1.459 倍、比解释器快 28.432 倍。
- 为全部六种数据向量类型新增直线与 CFG IR 的有界 128 位向量加载和存储，复用执行上下文区域来源、整次传输的边界/写权限/对齐预检、可诊断退出点、volatile 语义、按 lane 应用的原生/小端/大端字节序、存储结果保持、优化器副作用和栈映射，并提供独立的 AArch64 NEON、x86-64 SSE2 与有界 RV64IMD 标量降级；已验证对齐与非对齐逐字节精确往返、只读/错位/越界写入的原子失败、CFG 整向量边传递、安装包、ASan/UBSan、确定性差分模糊测试、覆盖 AArch64/Ubuntu/Windows x86-64 的完整 13 任务托管流水线、另一台真实 Ubuntu x86-64 主机和真实 Bianbu RISC-V 64。
- 新增按目标档案隔离且不生成代码、不分配可执行内存的直线与 CFG IR 降级能力预检，将完整已验证操作集分类为 `native`、`legalized`、`scalarized`、`helper` 或 `unsupported`，并报告必需特性位、向量宽度、执行上下文需求、各策略操作计数及各向量操作类别的寄存器/栈资源；编译阶段会对优化后的实际 IR 重新检查，并通过已编译函数与缓存句柄发布不可变遥测，同时覆盖目标/IR 失效关闭、被优化消除的操作、安装包消费者、包含 Windows MSVC x86-64 的完整 13 任务托管流水线、另一台真实 Ubuntu GCC 13.3 x86-64 主机和真实 Bianbu GCC 14.2 RISC-V 64 验证。
- 为直线与 CFG IR 新增自然对齐 8/16/32/64 位值的有界生成代码原子语义，覆盖加载、存储、交换、比较交换、fetch-add/and/or/xor、栅栏、全部合法 relaxed/acquire/release/acq_rel/seq_cst 内存序组合、副作用前的只读与边界检查、比较交换失败序、解释器执行、副作用安全优化、可诊断退出、栈映射活跃性及按目标档案失效关闭的能力遥测；新增采用 TSO 加载/存储映射、`XCHG`、`LOCK CMPXCHG`、`LOCK XADD`、有界按位比较交换循环和 `MFENCE` 的独立 x86-64 原生降级；新增按目标档案选择 LSE CAS/SWP/LDADD/LDCLR/LDSET/LDEOR 或 acquire/release 加载存储与最多 16 次 LL/SC 后转入无锁原子进度 fallback 的 AArch64 降级；并完成 RISC-V `A` 档案降级，以有限 32/64 位加载/存储/栅栏与 AMO 序列、最多 16 次 LR/SC 比较交换，以及保持相邻字节和跨 helper 活跃值的精确宽度 8/16 位 helper 覆盖完整语义；已在真实 Apple AArch64 基线/LSE、另一台真实 Ubuntu x86-64 GCC/Clang、Windows MSVC x86-64 与真实 RISC-V 64 `A` 主机上验证基线/优化执行、全部宽度/内存序/失败路径、CFG 边、全宽与子字争用更新、release/acquire 消息传递、seq_cst 存储缓冲及竞争比较交换，并通过 Linux ASan/UBSan、真实 RISC-V UBSan、扩展 5000 轮机器可读 litmus 记录、完整 13 任务托管流水线，以及托管平台矩阵和真实 RISC-V 64 上的安装包消费者验证。
- 为直线与 CFG IR 新增函数自有的非执行数据补丁单元，覆盖 value/target/shape/generation/counter 描述符、验证器形状与独立资源上限、初始快照参考解释、保留副作用的优化、基线与优化降级、acquire 读取、release 发布、强 acquire-release 比较交换、acquire-release fetch-add、不可变元数据与统计、禁止绕过原始原生入口的托管执行上下文绑定、可通过代次稳定缓存句柄变更且失效后由保留租约延续的生命周期，以及无需改写 RX 代码页的独立 x86-64 TSO、AArch64 `LDAR` 与 RISC-V 64 加载后栅栏降级；同时补齐安装包覆盖、保留 JSON 证据并接入 ThreadSanitizer 的 release/acquire 发布与竞争读写压力验证，以及补丁调用不超过等价常量编译调用 2.5 倍且原生代码不超过 128 字节的跨平台托管调用性能门槛；完整契约已在真实 Apple 与托管 AArch64、另一台真实 Ubuntu GCC 13.3 x86-64、托管 Ubuntu GCC/Clang 与 Windows MSVC x86-64，以及真实 RISC-V 64 GCC 14.2 硬件上通过基线/优化执行、10000 次发布与 80000 次争用更新、安装包消费者、Linux ASan/UBSan、ThreadSanitizer、真实 RISC-V UBSan、各平台留存性能判定和完整 13 任务托管流水线验证。

## 0.2.3

- 新增不可变且经过验证的目标特性档案，覆盖架构、ABI、字节序、必需标量/向量特性、向量宽度策略、主机能力探测、编译兼容性、已编译函数元数据、稳定档案键和按档案隔离的代码缓存发布；x86-64 探测结合 CPUID 与 XGETBV，RISC-V 64 记录 Linux 硬件能力，不兼容的跨目标或主机特性请求会在 W^X 发布前失败。
- 为直线与 CFG IR 新增有界类型化 Word 内存，包含执行上下文声明区域、动态无符号边界与绝对对齐检查、只读约束、8/16/32/64 位有符号或无符号加载与存储、原生/小端/大端字节语义、带活跃栈映射恢复的可诊断退出、副作用保留优化、资源上限，以及独立的 AArch64、x86-64、RISC-V 64 原生降级。
- 扩展有界内存以支持通过 Float64 SSA 类型转换的 Float32 存储、保持位模式的 Float64 存储、精确的有符号零与字节序行为、可独立折叠的 16/32/64 位 Word 字节翻转、对齐与逐字节精确的非对齐路径、确定性跨主机基准输入，以及在 AArch64、Ubuntu/Windows x86-64 和真实 RISC-V 64 硬件上校验和一致的解释器/原生性能记录。
- 为两种 IR 新增不可取址的受控 Word 与 Float64 帧局部，包含每次调用零初始化、类型化加载与存储、跨 CFG 边的优化器副作用保留、独立编译上限与统计、三个后端上符合 ABI 对齐的原生布局，以及在所有正常或可诊断原生返回路径上可选的敏感槽清零。
- 新增可信运行时对象布局，包含稳定语义布局身份、有界原生记录尺寸、固定自然对齐的 Word/Float64 字段、位置化执行上下文绑定、任何原生副作用前的整函数身份/尺寸/对齐/写权限预检、只读纯加载支持、无法通过公开原始入口绕过的托管入口约束、保守优化器顺序、资源上限，以及无需逐字段动态分支的 AArch64、x86-64、RISC-V 64 固定偏移直接降级。
- 在只审计 SLJIT 能力清单而不采用其实现的前提下深化独立商业低层路线图，明确严格可移植 SIMD lane 类型与顺序、广播、shuffle、有符号/无符号扩展和 lane 符号掩码、`native`/`legalized`/`helper`/`unsupported` 能力预检、标量化遥测、目标特性需求、生成代码原子内存序与比较交换规则、不可变数据补丁单元、私有快速/尾调用、版本化 C17 嵌入和经过验证的可移植 IR/AOT 产物，同时保持物理寄存器仅属于 MIR 且已发布代码始终为不可变 RX。
- 将新标量来源能力的资格验证扩展到基线与优化编译、两种参考解释器、验证器/预检负向用例、确定性差分模糊测试、缓存与调度器压力、Linux ASan/UBSan、Windows MSVC ASan、ThreadSanitizer、原生 macOS AArch64 与 Rosetta x86-64、真实 Ubuntu GCC x86-64、真实 Windows 11 MSVC x86-64 和真实 Bianbu RISC-V 64，并要求发布前完整托管 13 任务质量流水线通过。
- 新增项目级 MIT 许可证，依据随附的版权与担保条款授权使用、修改、分发、再许可和销售 UniJIT。

## 0.2.2

- 为 stock Lua 5.5 新增接受动态守卫 start、limit、step 的 Float64 数值 `for` 编译，针对小数、无穷、NaN 和两种有符号零保持精确的方向相关进入与继续语义；新增进入原生代码前的零步长错误、字节码活跃状态裁剪、逐源码迭代轮询的标量基线、保持 Lua 有序加法的方向专用八路索引组、受限标量尾部、实时分层差分测试，以及保留的三引擎完整循环平衡采样目标基线。
- 将 AArch64 标量 Float64 分配从六个寄存器扩展到 ABI 安全的全部调用者破坏 SIMD/FP 寄存器 `v0`–`v7` 与 `v16`–`v29`，继续保留 `v30`/`v31` 作为 lowering 临时寄存器、排除被调用者保留的 `v8`–`v15`，并在 helper 调用附近保护活跃值；同时记录了可移植 SIMD 交付前所需的类型化寄存器库与 ABI 角色基础。
- 将 System V x86-64 标量 Float64 分配从 XMM1–XMM4 扩展到全部十四个非 scratch 的调用者破坏寄存器，同时在实现 XMM6–XMM15 非易失保存前继续保留 Windows x64 的共同易失下限，从而消除宽 Float64 CFG 分组中可避免的溢出压力且不削弱任一 ABI。
- 新增 x86-64 专用的 CFG Float64 最后一次使用左操作数寄存器转交策略，使递推 SSA 算术可直接映射到破坏式二地址 SSE2 指令而无需前置寄存器复制，同时让 AArch64 与 RISC-V 64 保持常规三地址分配；新增链式递推的分配级回归覆盖。
- 修复 AArch64、x86-64 与 RISC-V 64 的 CFG 栈映射刷新逻辑，使持久跨块 SSA 值继续使用在定义点和边上写入的规范栈副本，避免以后续块内已被重新分配的物理寄存器覆盖；新增包含八个重复 Float64 边实参、守卫、安全点、寄存器压力及双向控制流的覆盖。
- 通过能力审计定义完全独立且不复用 SLJIT 实现的商业低层路线图，覆盖目标特性档案、有界类型化与字节序内存、采用 AArch64 NEON/x86-64 SSE2/RISC-V RVV 或已验证标量化的严格 128 位 SIMD、生成代码原子操作、JIT 内部调用与已验证尾转移、有界帧局部、配合纯数据补丁单元的不可变 RX 代码、不透明 C17 嵌入 ABI、可移植 IR 包、可信原生 AOT 对象及真实主机交付门禁。

## 0.2.1

- 将 AArch64、x86-64 与 RISC-V 64 的 CFG 原生栈帧从按 Value ID 扩张改为紧凑的跨块固定槽位与可复用的块内溢出、调用保护、退出捕获和边复制临时区，在保持规范栈映射偏移及类型化并行复制的同时，使数千节点的低压力图和 Lua 八路优化分支仍可满足 RISC-V 有符号 12 位栈寻址；新增超过 2,048 节点的基线回归，并取得 AArch64、Rosetta x86-64 与真实 RISC-V 64 原生执行证据。
- 为直线与 CFG SSA 新增有符号 64 位 Word `<`、`<=`、`==`、`!=`，保证规范化的零或一结果并补齐验证器类型与支配关系、解释器语义、常量折叠、自比较恒等式、死代码清理、值编号、寄存器分配，以及独立的 AArch64 `CMP`/条件选择、x86-64 `CMP`/`SETcc` 和 RISC-V 64 `SLT`/`XOR` 降级；为 stock Lua 5.5 数值循环启用覆盖全部六种有序/相等源码谓词的结构化整数 `if/else` 算术，在标量基线与八路优化层中精确合并 SSA 分支状态，并通过实时晋升覆盖有符号边界的 stock 运行时差分测试、确定性模糊测试、安装包测试，以及保留 stock Lua 1.25 倍和 LuaJIT 1.10 倍门槛的独立三引擎平衡采样商业门禁。
- 为直线与 CFG SSA 新增精确的有符号 64 位 Word 向下取整除法和取模，以确定性的零除数全函数语义避免宿主硬件陷阱，并补齐有副作用的 Word 非零守卫与诊断退出、验证器类型与支配关系、位精确解释器、常量折叠、死代码清理、值编号、寄存器分配，以及包含 `INT64_MIN / -1` 的独立 AArch64 `SDIV`/`MSUB`、防陷阱 x86-64 `IDIV` 和 RISC-V 64 `DIV`/`REM` lowering；在 stock Lua 5.5 的直线及数值循环基线/优化编译中启用 `OP_MODK`/`OP_IDIVK`/`OP_MOD`/`OP_IDIV`，保持精确的 `luaV_mod`/`luaV_idiv` 舍入语义、可重复 lowering 的唯一守卫站点、精确的除法/取模零除错误、跨实时晋升的有符号边界差分测试、确定性模糊测试和安装包覆盖。

## 0.2.0

- 为直线与 CFG SSA 新增精确的带符号双向 64 位 Word 移位，规定非负量左移、负量逻辑右移以及包含 `INT64_MIN` 在内的超宽量归零，并补齐验证器类型与支配检查、位精确解释器、常量折叠、零恒等式、死代码清理、块内值编号、寄存器分配，以及独立且带范围保护的 AArch64 `LSLV`/`LSRV`、保留活跃 `RCX` 的 x86-64 `SHL`/`SHR` 和 RISC-V 64 `SLL`/`SRL` 降级；在直线与数值循环基线/优化编译中启用 Lua 5.5 `OP_SHLI`/`OP_SHRI`/`OP_SHL`/`OP_SHR` 并精确匹配 `luaV_shiftl` 语义，保留由常量推导的循环分析，通过实时晋升对照原版 Lua 覆盖有符号边界，并扩展确定性模糊测试与安装包验证。
- 为直线与 CFG SSA 新增精确 64 位 Word AND、OR、XOR，覆盖验证器类型与支配关系检查、逐位精确解释器语义、常量折叠、恒等/零化简化、无用代码清理、块内值编号、寄存器分配，以及独立的 AArch64 `AND`/`ORR`/`EOR`、x86-64 `AND`/`OR`/`XOR` 和 RISC-V 64 `AND`/`OR`/`XOR` 降级；在 Lua 5.5 直线及数值循环的基线/优化编译中启用动态与常量形式的 `OP_BAND`/`OP_BOR`/`OP_BXOR`，保留由常量推导循环设置的分析能力，以 stock Lua 对照贯穿实时升级验证两端有符号边界，并扩展确定性模糊测试与已安装包覆盖。
- 为直线与 CFG SSA 新增精确 64 位 Word 取负和按位取反，覆盖模 2^64 的 `INT64_MIN` 语义、验证器类型与支配关系检查、常量折叠、成对一元运算消除、无用代码清理、块内值编号、寄存器分配，以及独立的 AArch64 `NEG`/`MVN`、x86-64 `NEG`/`NOT` 和 RISC-V 64 `SUB`/`XORI` 降级；在 Lua 5.5 直线及数值循环的基线/优化编译中启用整数 `OP_UNM` 与 `OP_BNOT`，并接入精确 Float64 `OP_UNM`，以 stock Lua 对照贯穿实时升级验证两端有符号边界及 Float64 有符号零，同时扩展确定性模糊测试与已安装包覆盖。
- 为直线与 CFG SSA 新增精确 IEEE-754 Float64 取负，覆盖仅翻转符号位的解释器与常量折叠语义、无用代码清理、块内值编号、寄存器分配，以及独立的 AArch64 `FNEG`、x86-64 整数符号掩码异或和 RISC-V 64 `FSGNJN.D` 降级；替换 QuickJS 与 PocketPy 的一元负号减法模拟，在基线与优化直接翻译中验证有符号零、无穷和自定义 NaN 载荷，并将有符号零检查贯穿计数循环、stock 运行时、确定性模糊测试及已安装包。
- 为直线与 CFG SSA 新增符合 IEEE-754 的 Float64 相等和不等谓词，覆盖精确 NaN 与有符号零语义、常量折叠、无用代码清理、值编号、寄存器分配，以及 AArch64、x86-64、RISC-V 64 上的独立原生降级；在基线与优化实时分层中为 QuickJS 数值启用 `==`、`!=`、`===`、`!==`，为 PocketPy 启用 `==`、`!=`，覆盖返回布尔值、循环条件和受守卫的 break/continue 循环体，并通过确定性模糊测试与已安装包资格验证。
- 新增公开的编译期 `measure_safepoint_polls` 策略，默认保持精确原生遥测，同时允许从延迟敏感代码中完全移除计数指令；Lua 因已从调用实参推导精确饱和循环热点而选择零开销路径，QuickJS 与 PocketPy 则保留实测回边，并覆盖直线/CFG 原生关闭路径及已安装包资格验证。
- 扩展生产级 x86-64 资格验证，使真实 Ubuntu GCC 与 Clang 主机现与 Lua、PocketPy、核心 JIT、性能门禁和已安装包消费者一起构建并执行 stock QuickJS 适配器，同时由 Linux ASan/UBSan 覆盖 QuickJS；因上游 stock QuickJS 依赖 GNU C 扩展，Windows MSVC 继续验证核心、Lua 与 PocketPy，由此既补齐 QuickJS 运行时此前仅在 macOS 上验证的缺口，也不虚假声明 MSVC 兼容性。
- 新增调用级安全点执行遥测，在 AArch64、x86-64 与 RISC-V 64 的解释器及原生代码中精确计数；QuickJS 与 PocketPy 现将实测计数循环回边计入饱和分层热点并通过统计 API 公开，可在单次 10,000 次迭代调用后异步升级，同时为直线代码保留 64 次调用策略，并覆盖中断、重置、循环计数及 stock 运行时资格验证。
- 新增 CFG 常量分支折叠与递归不可达块移除、折叠后无用值清理、不可达调用/守卫/栈映射/反优化记录的精确裁剪，以及受副作用边界约束的块内常量和有序纯表达式值编号；公开优化统计，并以选中路径语义、重复消除、基线元数据保留、优化层元数据裁剪、原生执行及扩展确定性差分模糊测试完成资格验证。
- 为 CFG SSA 新增生产级基线与优化编译，覆盖常量折叠、Word 规范化、死代码消除、副作用保留、反优化捕获值根化与重映射、公开 `CompilationOptions` 重载和已安装包验证；为 QuickJS 与 PocketPy 计数循环启用 64 次调用后的真实异步升级，覆盖分层缓存、代次安全发布、完整生命周期遥测，以及 PocketPy 优化层中的有符号零除法重建。

## 0.1.9

- 强化 Lua 5.5 在 Windows x86-64 上的商业交付，将持有异常析构状态的 C++ 编译过程与 Lua 的 `longjmp` 错误桥隔离，消除优化 MSVC 构建拒绝整数及 Float64 源码时的堆损坏，将内嵌资格程序拆分到 MSVC 字面量限制以内并保留阶段诊断，在全部真实平台与 Linux sanitizer 任务中启用 Lua，并新增带超时边界的原生 MSVC AddressSanitizer 流水线。
- 为 Lua 5.5 数值 `for` 循环体新增单层结构化整数条件，支持 `==`、`~=`、`<`、`<=`、`>`、`>=`、精确 SSA 状态合并、受条件控制的算术、`break` 和单返回值提前 `return`，覆盖归纳变量退出守卫的精确热点、紧凑标量基线、带有界尾部和安全点的独立受守卫八路优化分组、广泛的 stock Lua 差分测试、对多重守卫和 `else` 分支的显式拒绝，以及保留 stock Lua 1.25 倍和 LuaJIT 1.10 倍门槛的三引擎平衡采样托管 x64 门禁。
- 为 Lua 5.5 数值 `for` 编译新增受守卫的运行时参数步长，覆盖进入原生代码前的精确零步长错误、与方向无关的无符号剩余迭代计数、使常规 x86-64 循环状态保持寄存器驻留的无用 limit 裁剪、标量基线、跨索引回绕与 `math.mininteger` 的八路优化分组和有界尾部、穷举式基线/优化 stock Lua 差分覆盖，以及独立的三引擎平衡采样托管 x64 商业门禁，并保留 stock Lua 1.25 倍和 LuaJIT 1.10 倍门槛。
- 为 Lua 5.5 双向数值 `for` 编译新增受守卫的运行时参数起点，同时保留非零常量步长、按调用实参精确计算的饱和热点、零次迭代语义、安全标量与八路展开执行，以及贯穿两端有符号边界的基线/优化 stock Lua 差分覆盖。
- 为 Lua 5.5 数值 `for` 编译新增任意非零整数常量步长，覆盖方向感知的精确零次迭代与饱和热点计数、溢出安全的标量续行、组步幅可表示时的条件式八路展开、含 `math.mininteger` 在内极端步长的安全优化退化，以及跨两端有符号边界与 stock Lua 对照的基线/优化资格测试。

## 0.1.8

- 新增带步长的计数循环，覆盖 QuickJS 前/后置 `++`/`--` 与有限非零字面量 `+=`/`-=` 归纳更新，以及 PocketPy 一至三参数 `range`，包括正向/反向严格边界、与 guard 形式 break/continue 的组合、零/动态步长显式拒绝，以及直接原生与 stock 运行时资格测试。
- 为 QuickJS 与 PocketPy 计数循环新增单层 guard 形式的 `break` 和 `continue`，将精确 Float64 局部状态携带到循环出口或共享归纳更新块，保留无 `continue` 热回边的原有形状，显式拒绝尚未支持的 `else` 与多语句形式，并覆盖直接原生执行和 stock 运行时执行资格测试。
- 为 CFG SSA 新增类型化且带副作用的运行时辅助调用，覆盖经验证的支配性扁平实参、精确解释器执行、有界编译资源计量、调用局部 Word/Float64 活跃性、寄存器与栈混合实参封送、AArch64、x86-64 System V/Windows 和 RISC-V 64 上符合 ABI 的原生降级、调用后 guard/safepoint 安全退出、循环与超额实参资格测试，以及已安装包覆盖。
- 在 AArch64、x86-64 与 RISC-V 64 上新增相互独立的 Word/Float64 CFG 寄存器类别，使循环携带浮点值在算术与类型化并行边之间保持驻留，同时保留精确守卫/栈映射捕获和超额栈回退，并在固定 29 节点工作负载上强制校验和一致、至少达到参考解释器 5 倍且原生代码不超过 400 字节的托管硬门禁。

## 0.1.7

- 新增固定工作线程的后台编译调度器，覆盖任务数与估算字节双队列预算、非阻塞接纳、带期限背压、精确身份与代次去重，以及有界防饥饿的紧急、普通和后台优先级。
- 新增可复制编译票据，覆盖排队/运行/终态、排队任务立即取消与容量回收、运行任务无锁协作取消、异常隔离、有界等待、当前/峰值资源遥测，以及确定性的排空或取消停机。
- 新增生产级编译调度压力测试，通过确定性屏障协调多个生产者，并发编译和发布原生代码，校验缓存驻留与执行、核对生命周期计数、留存机器可读记录、在 ThreadSanitizer 下运行，并通过已安装 CMake 包消费公开 API。
- 接入 QuickJS 真实基线到优化分层，使已接受的直线 callable 立即发布低延迟基线代码，在 64 次调用后抢占优化，通过与运行时隔离的有界工作线程编译，复用精确源码优化缓存，拒绝陈旧代次，并在垃圾回收时取消排队任务。
- 通过 `unijit.stats`、`unijit.wait` 与 `unijit.cancel` 新增 QuickJS callable 遥测、有界等待和显式取消，并以确定性测试覆盖不同编译级别、异步升级、调度完成、不可分层 CFG 报告与可执行代码保留生命周期。
- 接入 Lua 5.5 整数与 Float64 真实分层，覆盖不可变数值原型快照、按数值模式和层级隔离的缓存、标量循环基线、八路展开优化循环、预期代次原子发布，以及在 64 次调用或 10,000 次实测循环迭代后由运行时隔离的后台编译。
- 通过 `unijit.stats`、`unijit.wait` 与 `unijit.cancel` 新增 Lua callable/循环遥测、有界等待、显式取消和 GC 取消，并以确定性测试证明调用触发的直线升级、由参数精确推导的回边计数、结构不同的优化循环 IR 与幂等终结。
- 将托管 UniJIT/LuaJIT 商业门禁强化为 300,000 次完整循环调用和 7 个样本，替换可能因共享 runner 调度噪声而翻转性能比值的毫秒级短采样。
- 将分层抢占接纳与代码发布纳入同一原子临界区，并在可执行代码可见前完成升级遥测，避免高速后台编译器触发冗余优化或暴露只更新一部分的统计状态。
- 将 PocketPy 优化编译从 VM 线程迁移到单工作线程调度器，以 64 个排队任务和 8 MiB 估算输入限制资源，覆盖不可变保留源码任务、精确缓存复用、协作取消、预期代次发布及 GC 安全的共享状态生命周期。
- 通过 `unijit.stats`、`unijit.wait` 与 `unijit.cancel` 新增 PocketPy 编译任务/调度遥测、带超时等待和显式取消，并以确定性测试覆盖冷态、异步升级、优化后受检除法、超时校验与外来对象拒绝。
- 新增带副作用的 CFG Float64 非零守卫，覆盖支配关系与类型校验、参考解释器中的精确有符号零退出、不可变反优化元数据、托管上下文供给，以及 AArch64、x86-64、RISC-V 64 上的独立原生降级。
- 为 PocketPy 计数循环启用受检 `/` 与 `/=`，以精确源码站点重建并将两种有符号零映射为 `ZeroDivisionError`，同时保留休眠除法不会执行的零次迭代语义。
- 为直线 SSA 新增返回 Word 的有序 Float64 `<` 与 `<=`，覆盖验证器、优化器、常量折叠、IEEE-754 无序结果为 false 的解释器语义、寄存器分配，以及 AArch64、x86-64、RISC-V 64 上的独立原生降级。
- 在编译函数与缓存租约中保留完整参数/返回类型，拒绝 CFG 不一致返回类型，并强化分层发布以逐项比较签名类型而非仅比较参数数量。
- 为 QuickJS 启用单个顶层 Number `<`、`<=`、`>` 或 `>=` 比较，严格拒绝链式比较，并返回在异步优化层升级后仍保持正确类型的真实 JavaScript Boolean。
- 为 PocketPy 启用单个顶层数值 `<`、`<=`、`>` 或 `>=` 比较，严格拒绝链式比较，并返回保留 NaN 与异步优化层语义的真实 PocketPy `bool`。
- 为每个已编译守卫和安全点新增规范化栈映射，覆盖精确直线活跃性、固定点 CFG 循环活跃性、后继参数到边实参的转换、稳定类型化帧槽、有界元数据增长，以及 AArch64、x86-64、RISC-V 64 上的独立发布。
- 通过编译函数和缓存租约公开不可变原生偏移、帧大小、退出类型、活跃 SSA 位置与映射遥测，强制运行时退出站点唯一，随已消除守卫裁剪映射，并通过已安装包 API 验证元数据生命周期。
- 新增每站点最多 64 个活跃值的无分配诊断退出捕获，在原生帧恢复前复制精确 Word/Float64 位，通过编译函数与代次稳定缓存租约进行校验重建，清理陈旧状态，并在超过固定容量时于编译期拒绝。
- 为反优化恢复新增任意 Word/Float64 SSA 输入，覆盖守卫作用域内的优化器保留与 Value ID 重映射、直线可用性与 CFG 支配关系校验、三后端强制分配器与数据流活跃性、规范捕获索引解析，以及基线、优化、缓存租约和安装包的精确重建。
- 物化 PocketPy 原生除法的基本类型帧状态，加入当前直线表达式左操作数以及全部计数循环局部变量和归纳值，同时保留既有参数与有符号零除数槽以兼容 `ZeroDivisionError` 映射。
- 新增绑定退出站点与恢复偏移的事务化对象物化，覆盖基本类型、常量、前向/后向及循环引用配方输入、有界图校验、两阶段对象外壳分配与字段填充、不透明前端句柄、每个回调失败阶段的严格单次回滚、显式对象逻辑槽，以及编译函数与代次稳定缓存租约 API。
- 将对象物化与逻辑解释器帧安装合并为一个原子前端事务，在 begin 阶段传递精确退出元数据与槽数量，提交前暂存类型化基本值和对象槽，并在安装失败时对对象图与帧严格执行一次联合回滚。
- 通过三轮轮换执行顺序的七样本测量稳定托管 Lua 商业门禁，以各轮中位数进行比较且不降低 stock Lua 1.25 倍或 LuaJIT 1.10 倍门槛，保留逐轮计时、执行顺序证据以及始终上传的结构化失败判定以供审计。
- 新增带容量上限且校验站点与恢复偏移的栈上替换入口，覆盖唯一类型化解释器槽、精确原生签名计划、成功路径到固定参数存储的无分配封送、代次稳定的编译函数与缓存租约转移、用于精确反优化的退出参数保留，以及已安装包验证。
- 将 OSR 转移接入原子分层快照，在并发发布期间保留实际尝试的基线或优化代码租约，在不破坏重建的前提下撤回发生假设退出的优化层，向全部前端统计 API 新增尝试/进入/退出遥测，并在代次切换过程中进行 OSR 压力验证。
- 新增可配置且必须为正的单次编译预算，覆盖参数、IR 节点与实参、CFG 块与边、退出及栈映射元数据和原生代码字节，在昂贵验证前与 W^X 发布前实施，并为 QuickJS/PocketPy 增加 1 MiB 未可信源码上限及公开安装包拒绝测试。
- 在元数据校验中强制每个版本的中英文发布说明条目数量一致，当任一单行 changelog 漏掉翻译更新时阻止标签发布。

## 0.1.6

- 新增显式的已验证直线代码基线与优化编译级别，使延迟敏感前端可在初始阶段跳过优化器，同时不绕过守卫、反优化元数据、原生分配或 W^X 发布。
- 接入 PocketPy 真实分层执行，覆盖精确源码保留、相互独立的基线与优化缓存、64 次成功调用后的原子单编译者升级、预期代次发布、失败延迟重试，以及直接保持单层的 CFG 循环。
- 新增 PocketPy 分层遥测以报告活跃代次、热度、编译结果、切换、代码大小与 IR 大小，保留实际尝试执行的代码租约以支持竞态安全退出重建，并验证优化升级后的 `ZeroDivisionError` 恢复。
- 新增公开的饱和调用与回边热度画像，支持可配置阈值、原子单编译者抢占、累计遥测，以及编译失败后的重试延迟以避免抢占风暴。
- 通过不可变且原子获取的快照新增无假设基线层与可替换优化层发布，覆盖不透明代次、签名校验、迟到结果拒绝、安全并发升级、预分配无内存分配降级和保留执行租约。
- 新增假设反优化后的自动优化层撤回与显式可重启基线重试策略，并覆盖并发切换、陈旧快照、回退、遥测、ThreadSanitizer 和安装包消费测试。
- 为直线与 CFG 编译新增一次性并发运行时假设，覆盖依赖站点校验、类型化入口帧恢复、托管入口与返回复核、与用户中断隔离的安全点唤醒，以及在受保护状态变更前阻塞等待静默。
- 将假设有效性接入原生代码缓存，使查询自动淘汰陈旧代次、同身份发布执行替换而非复用、保留租约安全反优化，并通过专用遥测报告自动假设失效。
- 扩展 ThreadSanitizer 运行时门禁，覆盖活跃原生循环假设失效、静默等待、帧重建、陈旧代次替换和独立粘性中断。
- 新增公开且不可变的反优化记录，覆盖语义原因、前端恢复偏移、入口参数/常量/守卫退出值的类型化恢复、编译器校验与优化裁剪、缓存租约生命周期保证，以及 ABI 边界帧重建。
- 在参考解释器以及 AArch64、x86-64、RISC-V 64 全部原生后端中保留可诊断守卫退出的精确 Float64 触发值位，包括零的符号。
- 将 PocketPy 受检除法接入经验证的反优化重建，只有确认的除零记录才映射为 `ZeroDivisionError`，未知运行时退出则明确诊断失败而不再被错误分类。
- 新增溢出安全的 Lua 数值循环八路展开、有界标量尾循环以及至多每八次源码迭代一次的协作式轮询，在已验证的 x86-64 完整循环工作负载上超过 LuaJIT，同时保留 `math.maxinteger` 边界语义。
- 将 Lua 完整数值循环对比提升为商业 CI 硬门禁，要求性能至少达到 stock Lua 的 1.25 倍和 LuaJIT 的 1.10 倍。
- 新增可按确定性种子重放的差分模糊测试，生成直线 Word/Float64 SSA 以及带循环和菱形分支的类型化 CFG 程序，并将生产原生执行结果与参考解释器逐位比较。
- 新增可配置的并发代码缓存压力测试，覆盖查询、发布、替换、精确与整键失效、LRU 淘汰、保留执行租约、清空和同步生命周期遥测。
- 新增职责明确的压力与差分模糊测试流水线，覆盖扩展固定种子语料、机器可读结果留存，以及并发缓存生命周期操作的 ThreadSanitizer 检查。
- 新增机器可读的商业性能硬门禁，以明确 CI 下限要求完整循环边界上的 UniJIT 执行超过 stock Lua、LuaJIT、stock QuickJS、V8 Jitless、stock PocketPy、CPython 3.14.6 解释器模式与 JIT 模式。
- 为 Lua 对比记录新增 UniJIT 相对 LuaJIT 的直接比值和测量边界元数据，并留存全部四份托管 Lua 基准产物。
- 修复 QuickJS 适配器诊断格式宽度，确保 Unix、MSVC 与 MinGW x64 工具链在警告即错误构建下均一致接受。
- 新增公开的线程安全有界 LRU 原生代码缓存，覆盖带指纹的身份、代次令牌、可执行映射字节预算、生命周期遥测、精确失效，以及在替换、淘汰、清空和缓存销毁后仍可安全执行的可复制租约。
- 将基于精确源码身份的原生代码复用接入 stock QuickJS 与 PocketPy 适配器，并通过运行时托管 callable 租约保留已编译映射。
- 新增安全的 Lua 5.5 数值原型缓存，身份覆盖数值模式、原型形状、完整指令流和精确数值常量位，隔离整数与 Float64 域，并为不支持的原型形状保留非缓存回退。
- 为 Lua 5.5 数值循环回边新增执行上下文安全点，使缓存的长时间运行函数仍可被协作式中断。
- 新增完整 QuickJS 数值计数循环编译，覆盖 Float64 局部状态、算术赋值、有序条件重置、SSA 合流、回边安全点、GC 托管原生调用，以及在已验证工作负载上超过 V8 Jitless 的位级精确目标基准。
- 新增完整 PocketPy `range(count)` 数值循环编译，覆盖缩进校验、Float64 局部状态、条件更新、SSA 合流、回边安全点、GC 托管原生调用，以及在已验证工作负载上超过 CPython 3.14.6 解释器和 JIT 模式的位级精确目标基准。
- 修复 CFG 编译元数据，确保包含安全点的函数始终通过安全调用 API 获得执行上下文。
- 修复多个目标参数共享物理寄存器时的 CFG 并行边复制，防止非局部循环状态在必需的栈发布前被覆盖。
- 新增有序 Float64 CFG 比较、IEEE-754 无序结果为 false 的语义，以及全部三种架构上的原生编码。
- 新增类型化 Float64 CFG 参数、常量、算术、循环携带边校验、解释器执行，以及 AArch64、x86-64 与 RISC-V 64 原生降级。

## 0.1.5

- 新增由单一源码驱动的 QuickJS、UniJIT、V8 Jitless 与 V8 语言内循环基准，包含固定 Node 执行环境、位级精确校验、引擎元数据与 CI 结果留存。
- 新增由单一源码驱动的 PocketPy、UniJIT、CPython 3.14.6 与 CPython 3.14.6 JIT 语言内循环基准，包含严格运行模式检查、官方包校验与 CI 结果留存。
- 将无 release 的 LuaJIT 参考依赖推进到上游 v2.1 分支最新提交，同时继续把所有参考构建生成状态限制在 `build/` 下。
- 消除可证明恒通过的 Float64 非零守卫，使 PocketPy 的已知非零常量除法继续使用无检查原生调用快路径。

## 0.1.4

- 为类型化 SSA 与全部原生后端新增 IEEE-754 Float64 除法，并接入受守卫保护的 Lua 5.5 和 QuickJS 前端。
- 新增覆盖加、减、乘、除的链式 Float64 常量折叠。
- 新增覆盖全部后端且可诊断退出的副作用 Float64 非零守卫，并以此为 PocketPy 原生除法保留 `ZeroDivisionError` 语义。
- 将托管自动化拆分为职责明确的持续验证、核心平台、语言基准、消毒器、发布元数据与发布工作流，不再使用笼统的 `ci.yml` 入口。

## 0.1.3

- 新增隔离的 PocketPy 2.1.8 构建、严格 Python 数值源码翻译、由 GC 管理生命周期的受守卫原生 callable，以及带校验和的 stock/UniJIT 调用基准。
- 新增隔离的 stock QuickJS 构建、严格数值函数到 Float64 SSA 的翻译、由 GC 管理代码生命周期的受守卫原生闭包，以及带校验和的 stock/UniJIT 调用基准。
- 新增显式执行上下文 ABI、无锁协作式中断、可诊断运行时退出，以及 AArch64、x86-64、RISC-V 64 上覆盖直线代码和 CFG 循环的副作用安全点。
- 新增可感知副作用的类型化运行时辅助调用、扁平 value-bits 参数、跨调用活跃值保护，以及所有目标架构上的原生 ABI 降级。
- 新增受守卫保护的 stock Lua 5.5 Float64 字节码编译，以及可复现的三引擎浮点调用基准。

## 0.1.2

- 新增类型化 Float64 SSA 常量与算术、IEEE-754 解释器 oracle、带优化的溢出处理，以及三个目标架构上的原生编码。
- 为全部原生后端新增 CFG 块内寄存器分配和可安全处理环路的并行寄存器复制，消除热循环中的栈往返。
- 将项目自有的前端、工具和文档目录统一改为单数形式：`frontend/`、`tool/` 与 `doc/`。
- 新增一个结构化 Lua 5.5 数值 `for` 循环的受守卫编译，覆盖 CFG 循环携带值、零次迭代语义与三引擎测试。
- 新增 AArch64、x86-64、RISC-V 64 上带范围校验的 CFG 原生降级，并以并行块参数复制正确处理循环回边。
- 新增使用显式块参数的 CFG SSA、支配关系验证，以及支持分支和循环并带执行预算的参考解释器。
- 新增 Lua 三引擎整数基准与锁定 ABI 的调用快路径；在本次调用边界工作负载上性能超过 stock Lua。
- 新增受守卫保护的 stock Lua 5.5 字节码到原生代码前端，首批支持直线整数函数，并配套差分与生命周期测试。
- 新增用于语言基准的隔离式 Lua 5.5 与 LuaJIT 参考构建。

## 0.1.1

- 新增经过验证的直线整数 SSA 表示与参考解释器。
- 新增自主实现的 AArch64、x86-64、RISC-V 64 原生代码生成器。
- 新增支持寄存器溢出的线性扫描分配器与 W^X 代码发布机制。
- 新增覆盖 macOS、Linux、Windows 的确定性差分测试。
- 新增可安装的 CMake 包元数据与跨平台 CI 矩阵。
- 新增规范化 SSA 优化流程与可复现核心基准。
- 新增十进制版本校验与由变更日志驱动的 GitHub Release 流程。
