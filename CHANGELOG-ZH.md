## 0.2.5

- 新增版本化C17嵌入ABI，提供不透明所有权、固定宽度结构、有界诊断、资源预算与异常隔离。
- 通过`unijit_v1_*`开放Word/Float64标量构建、guard、safepoint、代次安全快速调用、补丁单元、编译、调用与缓存。
- 将共享库限制为精确的67符号C清单，并新增纯C ABI、导出泄漏和安装包测试。
- 新增Ubuntu GCC x86-64、Windows MSVC x86-64与Apple Clang AArch64共享库专项验证。
- 新增可移植IR版本1，可确定性重建已验证的直线与CFG函数，且不序列化C++布局或进程地址。
- 使用固定小端记录、SHA-256负载完整性、分配前限额、严格长度检查与解码后验证保护可移植IR。
- 新增字节一致的可移植IR往返、破坏与截断负向测试、侧表覆盖、原生语义检查和安装包消费者。
- 按开源项目首页重构README，补齐支持状态、快速开始、示例、资格验证、文档、贡献与许可证入口。

## 0.2.4

- 为直线与CFG IR新增面向上下文无关Word/Float64目标的代次安全JIT到JIT类型化调用。
- 新增目标表原子发布、精确编译代次租约、缓存安全失效和快速调用并发切换。
- 新增严格128位SIMD语义核心，覆盖六种数据向量、规范mask、lane操作、比较、选择、shuffle与扩展。
- 为向量值补齐直线与CFG SSA、验证、解释、优化、分配、溢出处理和边复制。
- 完成已交付SIMD操作面的AArch64 NEON降级，包括有界I64x2乘法合法化。
- 完成System V与Windows x86-64 SSE2降级，包括对齐溢出槽和有界标量合法化。
- 新增不依赖或宣称RVV的有界RV64IMD标量SIMD降级。
- 新增有界对齐与非对齐128位向量内存，支持逐lane字节序与失败原子传输。
- 新增无需生成代码的目标档案能力预检和已编译降级遥测。
- 新增有界生成代码原子操作，覆盖显式内存序、目标原生路径、回退与litmus验证。
- 新增函数私有非执行补丁单元，支持release/acquire发布、CAS、fetch-add与缓存代次生命周期。
- 新增SIMD、补丁单元与快速调用性能门槛，以及Sanitizer、压力、安装包和真实主机验证。

## 0.2.3

- 新增不可变目标档案，覆盖架构、ABI、字节序、标量/向量特性和向量宽度策略。
- 为两种IR新增有界8/16/32/64位Word内存，支持显式区域、对齐、字节序和可诊断退出。
- 新增Float32/Float64内存、精确字节翻转、符号零保持与跨字节序差分测试。
- 新增调用级零初始化的不可寻址Word/Float64帧局部量、类型化访问和敏感槽清理。
- 新增调用绑定的可信对象布局，提供语义身份、固定字段、权限预检与托管入口。
- 在仅把SLJIT作为能力参考进行审计后，形成独立商业路线图。
- 为新来源模型扩展真实主机、Sanitizer、安装包、负向验证与资源限额资格测试。
- 新增仓库级MIT许可证。

## 0.2.2

- 新增原版Lua 5.5 Float64数值循环编译，支持动态起点、终点、步长、方向检查与八路展开。
- 将AArch64 Float64分配池扩展到全部ABI安全的调用者破坏SIMD/FP寄存器。
- 将System V x86-64 Float64分配池扩展到14个非scratch XMM寄存器，同时保留Windows ABI池。
- 为x86-64破坏性二地址CFG Float64算术新增末次使用左操作数捐赠。
- 修复AArch64、x86-64与RISC-V 64 helper调用后的规范跨块栈映射副本。
- 扩展独立低层路线图，覆盖SIMD、原子、补丁单元、快速调用、可移植IR与AOT。

## 0.2.1

- 以紧凑跨块槽和可复用溢出、调用、退出及边临时区替换按value ID增长的CFG帧。
- 为两种IR、优化器、解释器与全部原生后端新增有符号Word比较。
- 为全部后端新增向下取整除法与取模，并定义确定的零除数和负操作数语义。

## 0.2.0

- 为两种IR和全部原生后端新增带超范围归零语义的有符号双向Word移位。
- 新增精确64位Word AND、OR、XOR、取负与按位取反，并统一优化器和解释器语义。
- 新增Float64取负、相等与不等，精确保留NaN、符号零和负载位语义。
- 新增可选原生safepoint轮询遥测，并向QuickJS和PocketPy开放调用级计数。
- 新增CFG基线与优化编译，覆盖折叠、规范化、副作用保持和死代码清理。
- 新增常量分支折叠、递归不可达块删除与精确侧表裁剪。
- 将真实Ubuntu和Windows x86-64验证扩展到核心与三种语言前端。
- 新增优化一致性、运行时退出、安装包和架构降级的目标无关测试。

## 0.1.9

- 在Windows x86-64上隔离Lua `longjmp`错误桥与拥有异常状态的C++对象。
- 为Lua数值循环体新增单个结构化整数条件，保持精确SSA合并与比较语义。
- 新增受guard保护的运行时参数步长、精确零步长错误与溢出安全回边计数。
- 为升序和降序循环新增受guard保护的运行时参数起点。
- 新增任意非零常量整数步长与方向相关的零迭代行为。

## 0.1.8

- 为QuickJS和PocketPy新增带递增、递减及有限字面量步长更新的跨步计数循环。
- 新增单层受guard保护的`break`与`continue`及精确循环状态合并。
- 新增类型化有副作用CFG运行时helper调用，覆盖支配检查、混合参数、ABI降级和安装包。
- 分离Word与Float64 CFG寄存器类别，并在三个后端保持循环浮点值驻留。

## 0.1.7

- 新增有界固定工作线程编译调度器，支持去重、优先级、截止期、取消、遥测与确定性关闭。
- 新增确定性多生产者调度压力，覆盖原生编译、缓存发布、安装消费与ThreadSanitizer。
- 为Lua、QuickJS和PocketPy新增异步基线到优化分层、缓存复用、取消、等待与遥测。
- 通过更长且顺序平衡的采样稳定Lua/LuaJIT门槛，同时保持原性能下限。
- 新增CFG Float64 guard、PocketPy循环除法检查、有序比较与精确类型签名保留。
- 新增规范guard与safepoint栈映射、有界活跃值捕获及代次稳定重建。
- 新增参数、常量、guard值、优化值重映射和缓存租约的类型化反优化恢复。
- 新增支持循环对象图、逻辑帧安装、提交与恰好一次回滚的事务化对象物化。
- 新增带类型化帧计划、代次稳定转移、并发分层切换与退出重建的有界OSR入口。
- 新增IR、CFG、元数据、代码字节与不可信前端源码的可配置编译预算。
- 为发布元数据验证新增中英文单行更新条数一致性检查。

## 0.1.6

- 为延迟敏感前端新增显式且经过验证的基线与优化编译级别。
- 新增热度档案、单编译器抢占、不可变代次切换、重试与撤回组成的实时分层管理。
- 新增并发运行时假设、静默失效、缓存退休与ThreadSanitizer验证。
- 新增不可变反优化记录，以及三个原生后端上的精确Float64 guard值恢复。
- 新增PocketPy除法检查与经过验证的`ZeroDivisionError`重建。
- 新增溢出安全Lua八路循环展开，并将Lua/LuaJIT结果提升为商业CI门槛。
- 新增确定性差分模糊测试、并发代码缓存压力、留存记录与专项压力流水线。
- 新增Lua、QuickJS、PocketPy、LuaJIT、V8与CPython对比目标的机器可读性能门槛。
- 新增有界代次感知LRU原生代码缓存和三个前端的精确源码复用。
- 新增完整QuickJS与PocketPy数值计数循环编译、CFG safepoint和基准门槛。
- 新增类型化Float64 CFG值、算术、比较、边验证、解释与三后端降级。
- 修复CFG上下文元数据和共享物理寄存器的并行边复制。

## 0.1.5

- 新增QuickJS、UniJIT、V8 Jitless与V8单源码循环基准，提供位精确验证和留存元数据。
- 新增PocketPy、UniJIT、CPython与CPython JIT单源码循环基准，验证运行模式和官方包。
- 推进滚动LuaJIT参考提交，并将生成的参考状态保留在`build/`下。
- 消除可证明通过的Float64 guard，使已知安全的PocketPy除法保持直接原生路径。

## 0.1.4

- 为类型化SSA、全部原生后端、Lua与QuickJS新增IEEE-754 Float64除法。
- 新增Float64算术与除法的链式常量折叠。
- 新增有副作用Float64非零guard和精确PocketPy零除退出。
- 将自动化拆分为职责明确的验证、基线、Sanitizer、元数据与发布流水线。

## 0.1.3

- 新增隔离的PocketPy集成，包含数值源码翻译、GC持有原生可调用对象与可复现基准。
- 新增隔离的QuickJS集成，包含Float64 SSA翻译、受guard保护的原生闭包与可复现基准。
- 在三个架构上新增执行上下文、协作式中断、可诊断退出与原生safepoint。
- 新增带值位参数、活跃性保持和原生ABI降级的类型化有副作用运行时helper调用。
- 新增受guard保护的原版Lua Float64字节码编译与三引擎调用基准。

## 0.1.2

- 新增类型化Float64 SSA、IEEE-754解释器oracle、溢出处理与三个目标的原生编码。
- 新增块内CFG分配和循环安全的并行寄存器复制。
- 将项目目录重命名为单数形式`frontend/`、`tool/`与`doc/`。
- 新增一个带CFG循环携带值和零迭代语义的原版Lua结构化数值循环。
- 在三个架构上新增带范围检查的CFG降级与循环回边参数复制。
- 新增带显式块参数、支配验证和有预算解释器的CFG SSA。
- 新增三引擎Lua整数基准和固定ABI原生调用路径。
- 新增带差分和生命周期测试的原版Lua整数函数字节码编译。
- 新增隔离的原版Lua与LuaJIT参考构建。

## 0.1.1

- 新增经过验证的直线整数SSA表示与参考解释器。
- 新增独立AArch64、x86-64与RISC-V 64原生代码生成器。
- 新增支持溢出的线性扫描寄存器分配与W^X代码发布。
- 新增覆盖macOS、Linux与Windows的确定性差分测试。
- 新增可安装CMake包元数据与跨平台验证矩阵。
- 新增规范SSA优化流程与可复现核心基准。
- 新增十进制版本校验与由changelog驱动的GitHub Release。
